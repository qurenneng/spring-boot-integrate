server:
  port: 8080

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost/music?serverTimezone=UTC&useSSL=false&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
##扫描的xml 路径和 实体类
mybatis:
  mapper-locations: classpath:mapper/**/*Mapper.xml,
  type-aliases-package: com.example.springbootapiintegrat.*.bean


## 打印sql日志:
logging:
  level:
    com.example.springbootapiintegrat: debug

sande:
  mid: 1111