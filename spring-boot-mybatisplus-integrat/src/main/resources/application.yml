spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost/user?serverTimezone=UTC&useSSL=false&allowMultiQueries=true
    driver-class-name: com.mysql.jdbc.Driver

server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapper/**/*Mapper.xml,
  type-aliases-package: com.example.springbootmybatisplusintegrat.bean


## 打印sql日志:
logging:
  level:
    com.example.springbootmybatisplusintegrat.mapper: debug
